## What does this change do, and why?

<!-- One or two sentences. Link an issue if there is one (Fixes #123). -->

## How was this verified?

<!-- e.g. added/updated tests, ran `pnpm build && pnpm typecheck && pnpm check && pnpm test`, manually ran the example package -->

## Checklist

- [ ] `pnpm build`, `pnpm typecheck`, `pnpm check`, and `pnpm test` all pass locally
- [ ] Tests added or updated for any behavior change
- [ ] Docs (`README.md`/`packages/*/README.md`) updated if this changes public behavior
